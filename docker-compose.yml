services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: speech-generator:latest
    container_name: speech-generator
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}   # passed in from host env or .env file
      - DB_PATH=/data/corpus.db
    volumes:
      - corpus_data:/data                         # persists SQLite DB across restarts
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  corpus_data:
